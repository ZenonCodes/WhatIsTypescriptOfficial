import React from 'react';
import {WordType} from '../types/word.types';
interface Props {
    word:WordType
    phonetic: WordType
}


const Word:React.FC<Props> = ({word, phonetic}) => {
    return (
       <></>
    )
}

export default Word